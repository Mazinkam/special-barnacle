# Orchestrator optimization — revised consolidated plan

Status: proposed execution scope; not executed by writing this document.

This document supersedes the three 2026-09-25 run1/run2/run3 goal files. They are historical context only, not additional requirements. Historical dashboard numbers and diagnoses are hypotheses to verify against current code and a consistent data snapshot, not acceptance targets.

## Objective

Make orchestration economics trustworthy, reduce unnecessary expensive-model work without weakening verification, support opt-in model comparisons, and integrate Forge's existing live QA runner. Implement in one isolated worktree with sequential verification gates; do not activate all changes together or claim savings without measured evidence.

## Workspace and execution safeguards

- Repository: /Users/abdulkarim/.local/share/agent-skills/hierarchical-agent-orchestrator.
- Inspect git status, HEAD, existing worktrees and in-flight branches first. Record the base revision. Create .worktrees/orchestrator-optimization on feat/orchestrator-optimization from current main. If either exists, do not overwrite, reset or repurpose it: choose an unused suffixed name and report it.
- Keep all implementation in that worktree. Preserve other checkouts and uncommitted work. Do not merge, push, install the modified extension globally, or change active profiles.
- Make phase-scoped commits. Use one integration owner for shared files. Parallelize only disjoint ownership; serialize shared-file changes. Prefer a single lead where coupling is high.
- Read repository instructions and current implementations before designing changes. Produce a concise implementation checklist with tests and dependencies. Reuse existing modules and controls rather than adding parallel frameworks.
- Use a consistent, read-only snapshot of ~/.local/state/coding-agent-orchestrator for analysis. Backfills and dashboard regeneration run only on a copy. Redirect all development/test telemetry to isolated storage; do not mutate production telemetry or active runs.
- Bound retries; if a mandatory test/review gate fails, stop dependent work and report partial completion. Unavailable external infrastructure is a documented verification gap, never a pass.

## Phase 1 — evidence audit and measurement repairs

1. Trace current producer → storage → aggregation paths for own and nested costs, run lifecycle, verification and pricing provenance. Classify each earlier allegation as reproduced, already fixed, unsupported, or missing instrumentation. Separate old imports, current orchestrated calls and interactive sessions, and distinguish code at HEAD from the version that produced the data.
2. Reconcile costs using stable call/attempt/parent identities. Determine whether each source is cumulative, incremental, parent-inclusive or own-only. Fix reproduced omissions or double counting. Keep provider-reported, estimated and unknown amounts distinct. Unknown cost is not zero. Retain explicit unknown-role attribution rather than guessing.
3. If needed, implement a dry-run-first, idempotent backfill using existing locks and atomic-write utilities. Prefer per-call evidence; use aggregate residuals only when demonstrably non-overlapping. Never book both an aggregate and its detailed children. Include provenance, backup/rollback instructions and ambiguous cases that cannot safely be reconstructed.
4. Repair terminal events for supported normal exit, failure, cancellation and shutdown paths. For hard crashes/machine loss, reconcile on restart using available durable ownership/liveness evidence. Never mark a live run dead solely because it is old. Preserve interrupted/unknown states and unknown elapsed time where evidence is absent.
5. Record factual verification evidence: check commands, tested revision, outcomes, review verdicts, artifacts, skipped/unavailable checks and delayed outcomes. Do not manufacture a numeric quality score from passing tests. Reuse a score only if its semantics and inputs are already established and valid. Missing quality evidence must keep adaptive decisions insufficiently supported; do not automatically enable routing or promotion.
6. Repair reproduced pricing alias/provenance defects. Preserve rate version/source and historical provenance. Unverified prices remain labelled unverified; do not silently reprice historical spend using today's catalog. Identify authoritative price-verification work still needed.

Gate: regression tests for reproduced defects; reconciliation and backfill tests covering duplicate delivery, partial child evidence, nested depth, retries and unknown values; lifecycle tests including restart recovery; baseline and regenerated reports from the same snapshot. Explain residual differences rather than targeting the historical ~$155 estimate. Review and pass before Phase 2.

## Phase 2 — opt-in efficiency and model experiments

All new routing/behavior changes default OFF or non-executing recommendation mode. Each has an independent switch. Do not modify current model primaries, review requirements, verification depth, retry-review floors or spend-cap actions.

1. Cheap evidence gathering: move applicable parent-owned recon before expensive planning, reusing existing scouts, exemptions and bounded evidence packets. Architect and leads reuse that evidence. Missing evidence is explicit. Broad exploration and follow-up research delegate to cheap scouts; allow targeted source/diff inspection by decision-makers. Small tasks must not acquire mandatory costly fan-out.
2. Delegation: measure own tool work versus delegation first. Add configurable budget guidance and a safe delegation path for broad recon/test execution. Investigate historical edit/write access and enforce intended tool permissions through supported runtime mechanisms. Do not add fragile shell-text policing or remove legitimate integration/verification abilities without a tested alternative.
3. Polling: replace unnecessary lead status polling with existing completion/event mechanisms. Preserve useful monitoring, cancellation and recovery; avoid new busy loops and timers.
4. Scoped leads: implement opt-in plan → integrate → report sessions with bounded structured handoffs containing decisions/rationale, constraints, file ownership, base/tested revisions, work/results, unresolved risks, verification evidence and artifact references. Keep full evidence retrievable. Validate handoffs and detect stale revisions. Fall back safely to the existing long-lived lead when continuation is unsafe. Do not restart workers or repeat tests merely because a phase changed. Keep independent review/QA gates intact.
5. File ownership: prevent parallel overlapping edits using existing ownership/scheduling facilities, including integration files. High coupling serializes work or uses one lead. Emit conflict/rework evidence only where observable; no fabricated zero rates or speculative classifications.
6. Telemetry: record per-phase/per-dispatch turns, measured context usage with provider-specific token semantics, own tool mix, delegated work, observable polling, retries and costs. Distinguish full-run wall time from summed worker time.
7. Model canaries: support gpt-5.6-sol as an opt-in candidate for eligible premium-tier roles, and gpt-6-sol for implementation_fast and implementation_strong. Preserve security-specific assignments and explicit user model overrides. These are comparison candidates, not primary replacements or automatic promotions. Defaults: 0% exposure until explicitly enabled.
   - Inspect current failover/model-resolution support. Reuse its catalog qualification and requirement checks if available. Do not copy unfinished work or introduce temporary duplicate policy. If prerequisites are absent, implement the isolated configuration/reporting seam and keep activation unavailable with a clear reason.
   - Resolve exact provider/model identifiers from the available catalog; do not assume availability or hard-code unsupported provider preferences. Validate role requirements, tools, context and reasoning support where required. Provider substitutions remain separately attributable.
   - Use stable run/capability assignment, log requested and actually executed models, baseline/candidate, cohort, policy/config version, fallback and attempt identity. Preserve assignment through retries and label deviations.
   - Avoid confounding: do not combine model and scoped-lead experiments in the same comparison by default. Compare matched task class/risk/complexity, repositories and policy versions; display sample sizes, coverage and immature delayed outcomes. Cost per verified outcome includes failed attempts and nested work. No quality-equivalence or savings claim from tiny samples. Document opt-in, rollback and stopping criteria.

Gate: baseline behavior unchanged with switches off; delegation, handoff completeness/staleness/fallback, ownership, event waiting, canary determinism, explicit overrides, qualification and cost-attribution tests; independent code review and full relevant suites. Document separate rollout experiments rather than enabling everything. No paid/live model experiments without explicit approval and a budget.

## Phase 3 — repository live QA using Forge's existing runner

1. Inspect current Forge scripts/qa/README.md, runner CLI, artifact schemas and repository instructions. Reuse its runner, sessions, slots, environment checks and reporting. Do not assume historical slot restrictions or invent CLI flags. No duplicate browser/scanner/stack manager.
2. Add a minimal explicit repository QA adapter/configuration. Treat repository command execution as trusted opt-in: validate schema, pass argv without shell interpolation, and respect runtime trust/approval controls. Do not execute arbitrary discovered commands automatically.
3. Integrate live QA as a tracked verification stage alongside existing generic checks for explicitly selected scopes; provide an explicit request/override, not only an unreliable user-facing-change heuristic. Do not replace unit/typecheck/lint with browser QA.
4. Prove the tested revision includes the candidate changes. If the runner requires a commit, use a local checkpoint in the implementation worktree; record its SHA and the tested tree. Never quietly test stale main or discard dirty changes. For orchestrator changes, use the runner's supported local runtime override when relevant, not an unrelated globally installed extension. If the changed component cannot be exercised by Forge, state that limitation.
5. Delegate environment and slot management to the runner's supported interface. Respect its budget, cancellation and cleanup ownership. Do not add a competing session timeout, seize slots, expose credentials or bypass safety guards.
6. Parse structured results into pass/fail/unavailable, findings and artifact references. Required live QA that is unavailable means unverified, not passed. Link QA session identities to orchestration cost and ingest without double counting. Preserve unknown cost where unavailable.
7. Test adapter configuration, safe argument handling, revision checks, supported preflight outcomes, result parsing, cancellation and deduplicated cost linking. Include fake-runner integration tests.
8. A real Forge smoke test is a separate gated validation: first show scope, exact revision/runtime under test, required environment, expected runner side effects and spend/session budget; obtain approval before launching. Use a low-risk journey without unrelated builds. If prerequisites or approval are absent, finish offline tests and report LIVE QA NOT VERIFIED with the precise missing step. Do not mark the overall integration fully verified.

## Final verification and report

Run current repository-prescribed Python, Bun, typecheck/lint and relevant integration checks; discover actual commands instead of assuming documentation is current. Have independent review examine accounting, safety, lifecycle and verification changes. Fix findings and rerun affected/full checks as appropriate.

Report:
- Branch/worktree/base revision and phase commits; no merge/push.
- Audit table: confirmed bugs, already-fixed behavior, instrumentation gaps and unsupported historical claims.
- Same-snapshot before/after accounting, pricing provenance, lifecycle and verification coverage, with unresolved uncertainty.
- Tests and review findings; blocked external validation explicitly separated from passing offline checks.
- Feature defaults (off/recommendation), model candidate configuration (0% by default), dependency blockers and staged activation/rollback instructions.
- Forge tested revision, runtime, artifacts and verdict if approved/run; otherwise remaining verification steps.
- No automatic production backfill, deployment, policy activation or claimed quality-preserving savings.
