# All-in-one — runs 1, 2 and 3 in a single orchestration

Replaces launching the three goal files separately. Source of truth for each phase remains:
- docs/superpowers/goals/2026-09-25-run1-telemetry-fixes.md
- docs/superpowers/goals/2026-09-25-run2-lead-efficiency-and-model-canaries.md
- docs/superpowers/goals/2026-09-25-run3-forge-live-qa-adapter.md

Paste into HUMAIN Terminal:

```
/orchestrate --task-class backend_refactor --complexity 10 --risk medium --lead-size large
Implement all three goal specs in docs/superpowers/goals/ in one run, in /Users/abdulkarim/.local/share/agent-skills/hierarchical-agent-orchestrator: 2026-09-25-run1-telemetry-fixes.md (Phase 1: A1–A4), 2026-09-25-run2-lead-efficiency-and-model-canaries.md (Phase 2: R1, B1–B6) and 2026-09-25-run3-forge-live-qa-adapter.md (Phase 3: Forge live-QA adapter). Read all three files first. Their EVIDENCE, item lists, QUALITY GUARDs and VERIFICATION sections are requirements. The overrides below replace their WORKSPACE and prerequisite lines.

WORKSPACE (overrides the per-file workspace lines): Create ONE git worktree at .worktrees/orchestrator-optimization on a new branch feat/orchestrator-optimization from current main (b61ee7e or later). Do all three phases there. Do not touch the main checkout, .worktrees/model-failover or .worktrees/modular-refactor, and do not merge or push. Do not create the per-phase branches named in the files.

ORDER AND GATES: Phases run in order, and each must pass its own VERIFICATION section before the next begins. Commit each phase separately on the branch (one or more commits per item, message prefixed with the item id, e.g. "A1:"), so a phase can be reverted on its own. Phase 2 measures against Phase 1's telemetry and Phase 3 feeds Phase 1's quality_evidence_score, so do not start either early. Items within a phase may run in parallel only when their ownerPaths do not overlap. bridge/extensions/orchestrator/index.ts is shared, so work on it one item at a time.

MODEL-FAILOVER OVERRIDE (Phase 2, B6): feat/model-failover is not merged, so do not depend on it or copy from it. Implement canary selection in a new module (for example bridge/extensions/orchestrator/model-canary.ts) and keep changes to models.ts and profile parsing to a minimal, isolated hook, to make a later merge with feat/model-failover easy. Until that merge, apply a local requirement check for canary candidates: tier not lower than the baseline, reasoning support, and context of at least 256K. Name the candidate as openai-codex/gpt-5.6-sol or openai-codex/gpt-6-sol with amazon-bedrock/global.openai.* as fallback, and never use humain-node/gpt-5.6-sol. Leave a TODO note describing how it maps onto rules.model_requirements once failover lands.

PHASE 3 ENVIRONMENT: The real Forge end-to-end check needs Docker and a free Forge QA slot 0. If either is unavailable, finish everything else in Phase 3 and report "live QA end-to-end: not run: <reason>". Never fake it, never clean another run's slot, and never modify /Users/abdulkarim/Documents/Projects/forge beyond what its QA runner creates.

GLOBAL QUALITY GUARD: Do not change model tiers or primaries (only canary shares), verification depth, review_after_fix rules, QA requirements or spend-cap actions. Every Phase 2 behaviour change sits behind a feature flag with a kill switch, defaulting to recommend or canary. Never write to the live state dir ~/.local/state/coding-agent-orchestrator: all backfills and dashboard regenerations run against a copy.

STOP CONDITIONS: If a phase cannot pass its verification after the allowed retries, stop there. Report what is done and what failed, and do not start the next phase.

REPORT: branch and worktree path. Per phase: changed files per item, commits, test results (Python and `cd bridge && bun test extensions/orchestrator`), and anything deferred. Phase 1: before/after dashboard figures from the state-dir copy (total spend, cost by role, coordination rate, run status counts, verification and duration coverage). Phase 2: feature flags and defaults, canary configuration, and how to compare canary and baseline cohorts. Phase 3: the Forge session path and verdict, or the reason it was not run.
```
